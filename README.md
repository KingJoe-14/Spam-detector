# Spam-detector
A machine learning project that classifies SMS messages as spam or ham using TF-IDF and scikit-learn (Naive Bayes, Logistic Regression, Linear SVM), with a Django web app for live predictions.
